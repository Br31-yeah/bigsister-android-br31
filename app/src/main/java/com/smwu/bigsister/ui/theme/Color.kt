package com.smwu.bigsister.ui.theme

import androidx.compose.ui.graphics.Color

// globals.css 기반 색상 변환
val Background = Color(0xFFFFFFFF)
val Foreground = Color(0xFF030213) // --foreground

val Primary = Color(0xFF030213) // --primary
val PrimaryForeground = Color(0xFFFFFFFF)

val Secondary = Color(0xFFF3F3F5) // --secondary (밝은 회색)
val SecondaryForeground = Color(0xFF030213)

val Muted = Color(0xFFECECF0) // --muted
val MutedForeground = Color(0xFF717182)

val Accent = Color(0xFFE9EBEF) // --accent
val AccentForeground = Color(0xFF030213)

val Destructive = Color(0xFFD4183D) // --destructive
val DestructiveForeground = Color(0xFFFFFFFF)

val Border = Color(0x1A000000) // --border (투명도 10%)
val InputBg = Color(0xFFF3F3F5) // --input-background

// App.tsx 및 Figma에서 추출한 포인트 색상
val PurpleActive = Color(0xFF8B8FD9) // 활성 탭 색상
val PurpleLight = Color(0xFFE3E4FA)  // 연한 배경색
val MintConfirm = Color(0xFF8FD9B3)  // 확인/저장 버튼


// ▼▼▼ [추가] Figma 디자인용 공통 색상 ▼▼▼
val PurplePrimary = Color(0xFF8B8FD9) // 포인트 보라색

val TextGray = Color.Gray             // 회색 텍스트
val GrayBg = Color(0xFFF2F2F7)        // 입력창 배경 회색